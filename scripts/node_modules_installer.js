const { execSync } = require("child_process");
const fs = require("fs");

const folder = "frontend";
const nodeModulesPath = `${folder}/node_modules`;

if (!fs.existsSync(nodeModulesPath)) {
  console.log(`\n📦 Installing dependencies for ${folder}...`);
  execSync(`npm --prefix ${folder} install`, { stdio: "inherit" });
} else {
  console.log(`✅ ${folder} already has node_modules — skipping install.`);
}
