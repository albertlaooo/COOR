{
  "name": "coor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare-all": "node scripts/node_modules_installer.js",
    "dev": "npm run prepare-all && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
